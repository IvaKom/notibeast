list(APPEND BEAST_TEST_SRC
  beast.t.cpp
)

get_filename_component(GP_DIR_FULL_PATH ${CMAKE_CURRENT_SOURCE_DIR}/.. REALPATH BASE_DIR ${CMAKE_SOURCE_DIR})
get_filename_component(GP_DIR_NAME ${GP_DIR_FULL_PATH} NAME)
get_filename_component(P_DIR_NAME ${CMAKE_CURRENT_SOURCE_DIR} NAME)
message(STATUS "CMAKE_SOURCE_DIR: ${CMAKE_SOURCE_DIR}")
message(STATUS "P_DIR_NAME: ${P_DIR_NAME}")
#get_filename_component(GP_DIR_NAME ${P_DIR_NAME} DIRECTORY)
message(STATUS "GP_DIR_NAME: ${GP_DIR_NAME}")
list(TRANSFORM BEAST_TEST_SRC PREPEND ${GP_DIR_NAME}/${P_DIR_NAME}/)
set(BEAST_TEST_SRC ${BEAST_TEST_SRC} PARENT_SCOPE)
